# The CKEditor 5 build packages still declare peers against an older
# @ckeditor/ckeditor5-* line, so a strict resolve fails. Pinning the flag here
# keeps `npm install`, `npm ci` and the Docker build on the exact same tree.
legacy-peer-deps=true
