/**
 * Runtime configuration.
 *
 * In development this file holds the defaults and Vite's VITE_* variables win.
 * In the Docker image it is regenerated at container start from the environment,
 * so the very same build can be pointed at any API without rebuilding.
 */
window.__APP_CONFIG__ = {
  API_BASE_URL: '',
  API_TIMEOUT: '',
  APP_NAME: '',
  APP_VERSION: '',
  FACEBOOK_APP_ID: '',
};
